- guiboard
	1) remove 'dead' gui cells or make them default to blank
	2) pool?
	3) eventually tie to cur.board (prob)

- Mouse capture; convert to g(ui)cell Coords
    0) fix the math
	1) creating gcells
		+ link with input queue
	2) Check if an array of dead cells would be better than 
    dynamically adding/removing cells

- Think about reworking stepping in the FXMLController
	1) maybe move guiupdate() here

- Threads
	1) sleep
	2) divide the work

- make cells/buttons always rectangular (resize).

- Finish the thing.